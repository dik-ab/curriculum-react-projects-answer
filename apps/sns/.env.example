VITE_API_URL="http://localhost:3000"
# Spring Boot版のようにREST APIとSocket.IOのポートを分ける場合だけ設定します。
# VITE_SOCKET_URL="http://localhost:8001"
# Laravel / Gin + GORM / Rails版のように素のWebSocketでDMを実装する場合だけ設定します。
# VITE_REALTIME_DRIVER="websocket"
